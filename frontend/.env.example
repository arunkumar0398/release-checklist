# URL of the deployed Railway backend (no trailing slash)
# Example: https://release-checklist-backend.up.railway.app
# Leave empty for local development (Vite proxy handles /api/* → localhost:3001)
VITE_BACKEND_URL=
